# config.ini
[OBJECT_DETECTOR_PARAMETERS]

# Threshold for object detection confidence score
OBJECT_SCORE_THRESHOLD = 0.65

# List of object IDs to detect
OBJECT_IDS = 1,2

# CUDA visible devices configuration (empty string means use default)
CUDA_VISIBLE_DEVICES = ''
